---
'@fictjs/compiler': minor
---

Expose lazy `transform`, `scan`, and `analyze` request functions from
`@fictjs/compiler/native`, backed by one validated OXC/Rust compiler binding
with no per-file legacy fallback.
