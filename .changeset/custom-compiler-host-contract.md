---
'@fictjs/compiler': patch
---

Export the analyze request environment policy for custom native hosts and validate
authoritative virtual package metadata with the same schema as package files.
Document and execute the strict host, metadata, and final-emission contracts against
the real compiler and installed ESM/CJS packages.
