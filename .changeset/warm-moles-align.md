---
'@fictjs/compiler': patch
---

Align the OXC runtime helper package with the exact OXC release compiled into
the native compiler, and fail release validation if the Rust and npm versions
drift again.
