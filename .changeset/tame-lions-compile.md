---
'@fictjs/compiler': minor
---

Add the explicit `@fictjs/compiler/legacy` compatibility entrypoint and migrate
the Babel preset and Vite rollback path to it ahead of the Rust-default package
root transition.
